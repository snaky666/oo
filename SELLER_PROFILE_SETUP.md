# إكمال البيانات الشخصية للبائع 📋

## ما الذي تم تطبيقه؟

تم إضافة **نموذج إكمال البيانات الشخصية** الذي يفرض على كل بائع ملء بيانات كاملة عند تسجيل الدخول الأول!

---

## تدفق العملية

```
البائع يسجل دخول
    ↓
نظام التحقق: هل `profileComplete = true`؟
    ↓
إذا كلا → إعادة توجيه إلى `/seller/profile`
    ↓
البائع يملأ جميع البيانات:
  • الاسم الكامل
  • اسم النشاط/المتجر
  • رقم الهاتف
  • المدينة
  • العنوان التفصيلي
  • الرقم الوطني/السجل التجاري
    ↓
اضغط "حفظ البيانات والمتابعة"
    ↓
تعيين `profileComplete: true` في Firestore
    ↓
إعادة توجيه إلى `/seller` (لوحة التحكم) ✅
```

---

## الملفات المضافة/المعدلة

### 1. `client/src/pages/seller-profile.tsx` (ملف جديد)
صفحة كاملة لإكمال البيانات الشخصية:
- فورم تفاعلي مع React Hook Form + Zod validation
- عرض الأخطاء بشكل فوري
- حقول إدخال للبيانات الشخصية
- dropdown للاختيار من المدن السعودية
- button لحفظ البيانات

### 2. `shared/schema.ts` (محدّث)
- تحديث `User` interface مع الحقول الجديدة
- إضافة `UpdateSellerProfile` schema للـ validation
- تحديد الحقول المطلوبة والقيود

### 3. `client/src/pages/seller-dashboard.tsx` (محدّث)
- إضافة guard في useEffect لفحص `profileComplete`
- إعادة توجيه تلقائية إلى `/seller/profile` إذا لزم الأمر

### 4. `client/src/App.tsx` (محدّث)
- إضافة route `/seller/profile` محمي (seller role فقط)
- استيراد `SellerProfile` component

---

## البيانات المطلوبة

كل بائع **يجب** أن يملأ:

| الحقل | النوع | الحد الأدنى | الملاحظة |
|------|------|----------|---------|
| الاسم الكامل | نص | 3 أحرف | مثال: أحمد محمد السلمي |
| اسم النشاط | نص | 3 أحرف | مثال: متجر الأغنام الفاخرة |
| الهاتف | أرقام | 7 أرقام | مثال: 0501234567 |
| المدينة | dropdown | من القائمة | الرياض، جدة، إلخ |
| العنوان | نص | 5 أحرف | مثال: شارع النيل، الحي الشرقي |
| الرقم الوطني | أرقام | 9 أرقام | السجل التجاري أو الهوية |

---

## كيفية الاستخدام

### للبائع الجديد:
1. سجل حساب جديد
2. سجل دخول
3. **سيتم إعادة توجيهك تلقائياً إلى صفحة إكمال البيانات**
4. ملأ جميع الحقول (كلها مطلوبة)
5. اضغط "حفظ البيانات والمتابعة"
6. ستنتقل إلى لوحة التحكم (Seller Dashboard) ✅

### للبائع الموجود:
- البيانات محفوظة بالفعل
- `profileComplete = true`
- لن يتم إعادة توجيهه إلى صفحة البيانات

---

## معالجة الأخطاء

**الأخطاء المحتملة:**

| الخطأ | السبب | الحل |
|------|------|-----|
| "الاسم الكامل يجب أن يكون 3 أحرف على الأقل" | اسم قصير جداً | أكمل الاسم |
| "رقم الهاتف يجب أن يكون صحيح" | رقم قصير | استخدم 10 أرقام على الأقل |
| "العنوان يجب أن يكون 5 أحرف على الأقل" | عنوان قصير | أكمل التفاصيل |
| "رقم الهوية/السجل يجب أن يكون 9 أرقام على الأقل" | رقم قصير | استخدم الرقم الكامل |

---

## في قاعدة البيانات (Firestore)

عند إكمال البيانات، يتم تحديث مستند `users/{uid}` مع:

```javascript
{
  uid: "...",
  email: "seller@example.com",
  role: "seller",
  fullName: "أحمد محمد السلمي",
  businessName: "متجر الأغنام الفاخرة",
  phone: "0501234567",
  city: "الرياض",
  address: "شارع النيل، الحي الشرقي، بجوار السوق",
  identityNumber: "1234567890",
  profileComplete: true,
  createdAt: 1700000000,
  updatedAt: 1700000001
}
```

---

## الفوائد 🎯

✅ **بيانات دقيقة**: كل بائع يجب أن يملأ بيانات كاملة  
✅ **ثقة الزبائن**: معلومات اتصال حقيقية وصحيحة  
✅ **إدارة أفضل**: الأدمن يملك معلومات كاملة عن البائعين  
✅ **سهل الاستخدام**: واجهة بسيطة وواضحة بالعربية  

---

## الخطوات التالية

- [ ] اختبار مع حساب بائع جديد
- [ ] التأكد من الإعادة التلقائية إلى صفحة البيانات
- [ ] اختبار validation والأخطاء
- [ ] التحقق من حفظ البيانات في Firestore
- [ ] اختبار النقل إلى `/seller` بعد الحفظ ✅
