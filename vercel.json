{
  "version": 2,
  "buildCommand": "npm run build && mkdir -p public && cp -r dist/public/* public/",
  "installCommand": "npm install",
  "public": "public",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/[...].ts": {
      "maxDuration": 30
    },
    "api/sheep.ts": {
      "maxDuration": 30
    }
  }
}
